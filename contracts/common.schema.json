{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://code2edge/contracts/common.schema.json",
  "title": "Common definitions shared across all Code2Edge tool schemas",
  "$defs": {
    "envelope": {
      "description": "Mandatory envelope fields present on every tool output",
      "type": "object",
      "required": ["schema_version", "tool", "source", "run_id", "timestamp"],
      "properties": {
        "schema_version": {
          "type": "string",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "description": "Semver of this schema, e.g. 1.0.0"
        },
        "tool": {
          "type": "string",
          "description": "Name of the MCP tool that produced this output"
        },
        "source": {
          "type": "string",
          "enum": ["mock", "real"],
          "description": "Whether this output came from a real implementation or a mock"
        },
        "run_id": {
          "type": "string",
          "description": "Unique identifier for this tool invocation (UUID recommended)"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "ISO 8601 UTC timestamp of when the tool ran"
        }
      }
    }
  }
}
