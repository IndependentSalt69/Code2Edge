# Code2Edge deployment — run-7d942761

Generated by IBM Bob via Code2Edge workflow, run run-7d942761.

- Target: STM32U585 (Arduino UNO Q)
- Model: DS-CNN KWS, 119,372 params, int8 (169.2 KB frozen artifact: `model_data.c/.h`)
- Preprocessing: `feature_extraction.c/.h` (Hann window, reflection-padded STFT, HTK mel filterbank, log, normalize)

Both files are copies of Person A's already-verified pipeline
(`src/pipeline/feature_extraction.c/.h`, `src/pipeline/model_data.c/.h` on
`main`) — there is no separate code-generation step in this workflow; Bob's
job is to orchestrate the analysis/approval/parity/benchmark pipeline and
prove correctness at each gate, not to author new C++ from scratch.

## Host parity — REAL, PASS

500-sample corpus, 4 stages (fft, mel, log, normalize): **2500/2500 PASS**,
`first_divergent_stage: null`. Source: `evidence/parity/host_parity_report.json`
via `tools/run_host_parity.py`. See `workflow/runs/run-7d942761/parity_host_attempt1.json`
for the full stage-wise result.

Caveat: `tools/run_host_parity.py` currently hardcodes
`src/pipeline/feature_extraction.c` as the file it compiles and tests — it
is not yet parameterized to test an arbitrary path — so this result verifies
Person A's committed pipeline (which this deploy branch copies byte-for-byte),
not an independent re-verification of this specific copy. Logged in
`docs/interface-requests.md`.

## Device parity — MOCK

No physical board was connected in this session.
`workflow/runs/run-7d942761/parity_device_attempt1.json` uses
`mock_scenario="pass"` and is clearly `"source": "mock"`. Real (but thin —
1 sample, 1 stage) device parity evidence already exists at
`evidence/parity/device_parity_report.json`; a full corpus-wide sweep does
not exist yet.

## Benchmark — MOCK

Same reason: `workflow/runs/run-7d942761/benchmark.json` and
`predicted_vs_measured.md` are `"source": "mock"`. No real physical DS-CNN
latency benchmark exists yet — only an infrastructure-validation run
(`evidence/benchmarks/benchmark_harness_physical_run.json`, explicitly
flagged there as not model latency).

**See `HARDWARE_VALIDATION_GUIDE.md` in this same PR for the exact steps
to replace the two mocked sections above with real hardware data.**

Data: real (host parity, edge readiness) / mock (device parity, benchmark)
